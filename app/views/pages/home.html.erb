<div class="background">
  <div class="text-center font-awesome-font">
    <h1>Handover Certificate</h1>
    <h2>Property Inventory Management Specialists</h2>
    <div class="button-container">
      <% if user_signed_in? %>
        <%= link_to "Properties", properties_path, class: "btn btn-dark" %>
        <%= link_to "Logout", destroy_user_session_path, data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "btn btn-danger" %>
    <% else %>
      <%= link_to "Login", new_user_session_path, class: "btn btn-primary" %>
      <%= link_to "SignUp", new_user_registration_path, class: "btn btn-secondary" %>
    <% end %>
    </div>
  </div>
</div>
