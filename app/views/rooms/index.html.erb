<h1>All Rooms</h1>

<% @rooms.each do |room| %>
  <div>
    <h2><%= room.name %></h2>
    <p><%= room.description %></p>
    <p><strong>Property:</strong> <%= room.property.name %></p>
  </div>
<% end %>
